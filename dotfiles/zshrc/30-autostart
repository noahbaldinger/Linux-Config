# -----------------------------------------------------
# AUTOSTART
# -----------------------------------------------------

# -----------------------------------------------------
fastfetch --config examples/13
# -----------------------------------------------------

if [[ -z "$TMUX" && "$TERM" == "xterm-kitty" ]] && command -v tmux &>/dev/null; then
  tmux new-session -A -s main
fi
